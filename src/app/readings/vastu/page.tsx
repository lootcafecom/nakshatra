import ComingSoon from "@/components/ComingSoon";

export default function VastuPage() {
  return (
    <ComingSoon
      roman="IV"
      title="Vastu"
      description="Direction and dwelling, read against the true magnetic compass of where you actually live — not a generic compass tip."
      week="Geocoding and directional engine arrive in Week 3"
    />
  );
}
